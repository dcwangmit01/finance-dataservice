<%= form_for(@ticker) do |f| %>
  <% if @ticker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticker.errors.count, "error") %> prohibited this ticker from being saved:</h2>

      <ul>
      <% @ticker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :security_type %><br />
    <%= f.text_field :security_type %>
  </div>
  <div class="field">
    <%= f.label :underlying %><br />
    <%= f.number_field :underlying %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
